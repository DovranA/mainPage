import './banner.css'

const Banner = () => {
  return (
    <div className='banners'>
      {/* <div className='banner'>
        <span className='title'>
          {data?.position + ' '}
          {data?.total ? '(' + data.total + ')' : ''}
        </span>
        <img
          src={'http://' + data?.image}
          alt={String(data?.total)}
          className='image-fit'
        />
      </div> */}
    </div>
  )
}

export default Banner
